Flag SVGs in `4x3/` are from the `flag-icons` project:

https://github.com/lipis/flag-icons

The assets are bundled locally so MyWhispr does not make network requests just
to render model language badges. The upstream project is MIT licensed.
